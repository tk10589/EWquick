<%= form_with model: @preparer, url: "/preparers", class: 'registration-main', local: true do |f| %>

<div class='form-wrap'>
  <div class='form-header'>
    <div class='form-header-text'>
      提出者情報
    </div>
  </div>

  <div class="user__wrapper">
      <%= render partial: 'preparers/preparer' %>
  </div>

  <div class='new-btn'>
    <%= link_to "提出者新規登録", new_preparer_path, method: :get, class: "new-red-btn" %>
    <%#= f.submit "提出者新規登録" ,class:"register-red-btn" %>
  </div>

</div>
<% end %>